const initialState={
  servicesInfoList: [
    {name: "Ruths Social Service", location: "123 Copley Square"}
  ]
}

const services = (state= initialState, action)=>{
  switch(action.type){
    default:
      return state
  }

}

export{
  services
}
